export type Grocery = {
  id: string
  // eslint-disable-next-line camelcase
  image_url: string
  stock: number
  productName: string
  price: number
  productDescription: string
  favorite: boolean
}
